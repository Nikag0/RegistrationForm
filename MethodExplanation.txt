ElementName
Возвращает или задает имя элемента, который будет использоваться в качестве исходного объекта привязки.
 //
 // Возврат:
 // Значение свойства Name или директивы x:Name интересующего элемента.
 // Вы можете ссылаться на элементы в коде, только если они зарегистрированы в соответствующем
 // System.Windows.NameScope через RegisterName. Для получения дополнительной информации смотрите WPF
 // XAML Namescopes. Значение по умолчанию равно null.

Path
Сводка:
 // Возвращает или задает путь к свойству источника привязки.
 //
 // Возврат:
 // Путь к источнику привязки. Значение по умолчанию равно null.

BindingExpressionBase SetBinding
//
 // Сводка:
 // Присоединяет привязку к этому элементу на основе предоставленного объекта привязки.
 //
 // Параметры:
 // dp:
 // Определяет свойство, для которого должна быть установлена привязка.
 //
 // привязка:
 // Представляет специфику привязки данных.
 //
 // Возврат:
 // Записывает условия привязки. Это возвращаемое значение может быть полезно для проверки ошибок
 //.